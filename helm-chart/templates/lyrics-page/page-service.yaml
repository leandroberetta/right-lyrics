apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}-lyrics-page
  name: {{ .Release.Name }}-lyrics-page
spec:
  ports:
    - name: tcp-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}-lyrics-page