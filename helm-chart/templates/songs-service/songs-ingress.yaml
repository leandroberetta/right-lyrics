apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-songs-service
spec:
  rules:
    - host: {{ .Release.Name }}.{{ .Values.wildcardDomain }}
      http:
        paths:
          - path: /api/songs
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-songs-service
                port: 
                  number: 8080
