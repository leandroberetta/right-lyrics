apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}-hits-redis
  name: {{ .Release.Name }}-hits-redis
spec:
  ports:
    - name: tcp-6379
      port: 6379
      protocol: TCP
      targetPort: 6379
  selector:
    app: {{ .Release.Name }}-hits-redis
