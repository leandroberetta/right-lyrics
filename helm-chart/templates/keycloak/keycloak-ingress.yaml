apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-keycloak
spec:
  rules:
    - host: {{ .Release.Name }}-keycloak-{{ .Release.Namespace }}.{{ .Values.wildcardDomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-keycloak
                port: 
                  number: 8080
