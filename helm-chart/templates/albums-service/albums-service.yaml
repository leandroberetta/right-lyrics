apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}-albums-service
  name: {{ .Release.Name }}-albums-service
spec:
  ports:
    - name: tcp-8080
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: {{ .Release.Name }}-albums-service