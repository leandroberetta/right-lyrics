apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Release.Name }}-albums-mariadb
  name: {{ .Release.Name }}-albums-mariadb
spec:
  ports:
    - name: tcp-3306
      port: 3306
      protocol: TCP
      targetPort: 3306
  selector:
    app: {{ .Release.Name }}-albums-mariadb
